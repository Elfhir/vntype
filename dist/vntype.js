(function(root, undefined) {

  "use strict";




var vntype = function() {
	var config = config || {};
};

// Version.
vntype.VERSION = '0.0.1';

// Export to the root, which is probably `window`.
root.vntype = vntype;


}(this));
